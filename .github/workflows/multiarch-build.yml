name: Multi-Arch Build & Verify
"on":
  push:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'src/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
  pull_request:
    branches: [main]
    paths:
      - 'Dockerfile'
      - 'src/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ─
  build-multiarch:
    name: Build (${{ matrix.platform }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform:
          - linux/amd64
          - linux/arm64
    steps:
      - uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Add eBPF build dependencies to Dockerfile
        run: |
          # Insert dependencies for eBPF/cargo-chef build into the builder stage
          if ! grep -q clang Dockerfile; then
            # Add clang, libbpf-dev, libelf-dev after the builder stage declaration
            sed -i '/^FROM .* AS builder/a RUN apt-get update && apt-get install -y --no-install-recommends clang libbpf-dev libelf-dev && rm -rf /var/lib/apt/lists/*' Dockerfile || true
          fi

      - name: Build image for ${{ matrix.platform }}
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: ${{ matrix.platform }}
          push: false
          load: true
          tags: stellar-k8s-test:${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
          cache-from: type=gha,scope=${{ matrix.platform }}
          cache-to: type=gha,mode=max,scope=${{ matrix.platform }}
          build-args: |
            BUILDPLATFORM=linux/amd64
            TARGETPLATFORM=${{ matrix.platform }}

      - name: Verify binary architecture
        run: |
          ARCH=${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
          docker run --rm --platform ${{ matrix.platform }} \
            stellar-k8s-test:${ARCH} \
            /stellar-operator version
          echo "v∓ stellar-operator boots on ${{ matrix.platform }}"

          docker run --rm --platform ${{ matrix.platform }} \
            stellar-k8s-test:${ARCH} \
            /stellar-fork-detector --help
          echo "✓ stellar-fork-detector boots on ${{ matrix.platform }}"

          docker run --rm --platform ${{ matrix.platform }} \
            stellar-k8s-test:${ARCH} \
            /stellar-watcher --help
          echo "✓ stellar-watcher boots on ${{ matrix.platform }}"

  publish-manifest:
    name: Publish Multi-Arch Manifest
    runs-on: ubuntu-latest
    needs: build-multiarch
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=edge

      - name: Build and push multi-arch manifest
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Inspect manifest
        run: |
          docker buildx imagetools inspect \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:edge

  arch-benchmark:
    name: Arch Benchmark (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
          - arch: arm64
            runner: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.arch }}-bench-${{ hashFiles('**/Cargo.lock') }}

      - name: Run reconciler benchmark
        run: |
          cargo test --release -- --nocapture 2>&1 | tee bench-${{ matrix.arch }}.txt
          echo "ARCH=${{ matrix.arch }}" >> $GITHUB_ENV
        continue-on-error: true

      - name: Upload benchmark results
        uses: actions/upload-artifact@v5
        if: always()
        with:
          name: bench-${{ matrix.arch }}
          path: bench-${{ matrix.arch }}.txt

  compare-arch-benchmarks:
    name: Compare ARM vs AMD Benchmarks
    runs-on: ubuntu-latest
    needs: arch-benchmark
    if: always()
    steps:
      - name: Download amd64 results
        uses: actions/download-artifact@v5
        continue-on-error: true
        with:
          name: bench-amd64
          path: results/

      - name: Download arm64 results
        uses: actions/download-artifact@v5
        continue-on-error: true
        with:
          name: bench-arm64
          path: results/

      - name: Print comparison
        run: |
          echo "## Architecture Benchmark Comparison" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### AMD64 (x86_64)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          if [ -f results/bench-amd64.txt ]; then cat results/bench-amd64.txt; else echo "No AMD64 results"; fi >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### ARM64 (Graviton / Apple Silicon)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          if [ -f results/bench-arm64.txt ]; then cat results/bench-arm64.txt; else echo "No ARM64 results"; fi >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
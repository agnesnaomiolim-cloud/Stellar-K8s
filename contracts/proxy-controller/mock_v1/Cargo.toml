[package]
name = "mock-v1"
version = "0.1.0"
edition = "2021"
publish = false
description = "Test fixture: v1 of a contract upgraded through proxy-controller"

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = "23.5.0"
proxy-controller = { path = "../proxy_controller" }
